# Database
DATABASE_URL=postgresql+asyncpg://buildleads:secret@localhost:5432/buildleads

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT (CHANGE IN PRODUCTION!)
JWT_SECRET=change-me-in-production-use-long-random-string-64chars
JWT_ACCESS_MINUTES=30
JWT_REFRESH_DAYS=7

# OSINT API keys (from Potencjal)
CEIDG_API_KEY=
GUS_API_KEY=

# Stripe (Phase 5)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_GROWTH=
STRIPE_PRICE_ENTERPRISE=

# Email — Resend (Phase 4)
RESEND_API_KEY=
EMAIL_FROM=BuildLeads <leads@buildleads.pl>

# AI — Claude Haiku (primary), Ollama (fallback)
ANTHROPIC_API_KEY=
OLLAMA_URL=http://localhost:11434

# Frontend
FRONTEND_URL=http://localhost:3000

# Platform admin seed
ADMIN_EMAIL=admin@buildleads.pl
ADMIN_PASSWORD=admin123

# Environment
ENV=development
